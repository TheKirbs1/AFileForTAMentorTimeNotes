import React, { useContext } from "react";
import { Context } from "../store/appContext";

const MentorHome = () => {
  // const { store, actions } = useContext(Context);

  return (
    <div className="bg-primary">Mentor Quick Details</div>
  );
};

export default MentorHome;
